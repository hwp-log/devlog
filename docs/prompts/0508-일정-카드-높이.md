# 0508 회고: 트랙 7 — 일정 카드 높이 고정

**작업일**: 2026-08-05
**관련 커밋**:
- `d6edd20` fix: 0508 일정 카드 높이를 고정 (2 files, +42 / -21)

---

## 1. 한 줄 요약

주소 유무·좌표 유무에 따라 들쭉날쭉하던 일정 카드 높이를, 주소 슬롯을 항상 확보하는 방식으로
통일하고 슬롯 판정 범위를 플랜 전체로 올려 Day 탭 전환에도 높이가 유지되게 했다.

---

## 2. 왜 / 목적 / 이유

- 이 흔들림은 트랙 2(0494 주소 줄 추가)와 트랙 5(0501 44px 터치 영역)가 겹쳐 생긴 것 —
  왜 개별 사이클에서 못 잡고 여기서 별건으로 잡았는지

---

## 3. 작성한 프롬프트

기록 없음.

---

## 4. 코드 작성 & 수정

### ① 슬롯 판정 범위를 호출자에게 (`PlanTimeline.tsx`)

```tsx
// 0508: 주소 슬롯 확보 여부 — 호출자가 플랜 전체(spots) 기준으로 전달하면 Day 탭 전환에도
//   카드 높이가 유지된다. 미전달 시 그날 items에서 파생(소유자 뷰는 주소가 없어 슬롯 없음).
reserveAddressSlot?: boolean;
```
```tsx
// 0508: 카드 높이 통일 — 주소가 하나라도 있으면 모든 카드에 주소 줄 자리를 고정 확보
//   (없는 카드는 빈 줄). 주소 데이터가 아예 없는 뷰(소유자 my-plan)는 슬롯 자체가 안 생겨
//   기존 그대로. 판정 범위는 호출자 prop 우선(플랜 전체 기준 — Day 탭 전환 시 높이 유지).
const hasAnyAddress = reserveAddressSlot ?? items.some((it) => it.spot.address);
```

### ② 18px 고정 슬롯 + 레이아웃 안 미는 터치 영역

```tsx
{/* 0508: 슬롯 높이 18px 고정(주소 없으면 빈 줄) — 카드 높이·이름 시작 위치 통일.
    모바일 터치 44px(CLAUDE.md §5)은 min-h-[44px] + -my-[13px]로 레이아웃 안 밀고
    확보(44-26=18px 점유) — 이전처럼 레이아웃을 밀면 링크 카드만 커져 높이가 갈라진다. */}
{hasAnyAddress && (
  <div className="mt-0.5 h-[18px] text-[12px] leading-[18px]">
    {spot.address && (spot.lat != null && spot.lng != null ? (
      <button ... className="flex max-w-full items-center gap-1 min-h-[44px] -my-[13px] sm:min-h-0 sm:my-0 ...">
```

0501이 `min-h-[44px]`만 걸어 링크가 있는 카드만 커지던 문제를, 음수 마진(`-my-[13px]`)으로
**시각 점유는 18px 유지 + 히트 영역은 44px**로 분리해 해소.

### ③ 호출부 (`PlanFinderDetail.tsx`)

```tsx
{/* 0508: 주소 슬롯은 플랜 전체 기준 — Day 탭을 넘겨도 카드 높이가 변하지 않게. */}
<PlanTimeline items={timeline} currency={currency} showAmount={false}
  reserveAddressSlot={spots.some((s) => !!s.address)} />
```

---

## 5. 결과 / 실측

- 슬롯 산식: 44 − 26(상하 13px씩) = **18px 점유**, 텍스트 줄높이 `leading-[18px]`와 일치.
- 데스크톱(`sm:`)은 `min-h-0 my-0`으로 compact 유지.
- 주소 데이터가 없는 소유자 뷰(`my-plan`)는 `hasAnyAddress=false`라 슬롯 자체가 안 생김 — 무영향.

### 이후 사이클 (사실 기록)

`PlanTimeline.tsx`는 **0558(`2721a5e`)에서 삭제**(0556 이후 소비자 0).
이 사이클의 슬롯 규칙이 후속 `PlanItemRow`로 어떻게 승계됐는지는 별도 확인 필요.

---

## 6. 배운점

