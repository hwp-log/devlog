# 0495·0497 회고: 트랙 3 — 플랜 대표 이미지

**작업일**: 2026-08-04 ~ 08-05
**관련 커밋**:
- `64ed449` feat: 0495 플랜 대표 이미지 부여를 담긴 장소 기준으로 개선 (4 files, +105 / -13)
- `eeca538` feat: 0497 플랜 대표 이미지를 작성자가 고를 수 있게 (4 files, +127 / -3)

---

## 1. 한 줄 요약

플랜 커버 선택의 1순위를 "담긴 Spot의 커버"로 올리고 region·movie 미입력 시 담긴 Spot에서
추론하게 한 뒤(0495), 그 후보를 작성자가 직접 고를 수 있는 선택 UI를 붙였다(0497).

---

## 2. 왜 / 목적 / 이유

- 왜 추론값으로 저장 필드(region·movie)를 덮지 않고 커버 선택에만 쓰기로 했는지
- 왜 후보가 **2개 이상**일 때만 선택 UI를 띄우는지 (코드 주석: "1개는 자동 결과와 같아 실효 없음")
- 왜 수정 시 커버 미접촉 원칙(생성 1회 부여)에 예외를 뒀는지

---

## 3. 작성한 프롬프트

기록 없음.

---

## 4. 코드 작성 & 수정

### 0495 — 커버 우선순위 재정의

**우선순위 1 신설** (`lib/plan/pick-cover.ts`):
```ts
// 0495 후속: 커버 우선순위 1 — 이 플랜이 담은 Spot의 커버(있는 것 중 order 최소).
//   첫 장소에 커버가 없어도 다음 커버 스팟으로 넘어가 히어로를 포기하지 않는다.
//   폴백(작품→지역)은 pickPlanCover가 담당(priority 2·3·4).
export function firstOwnSpotCover(spots: { order: number; coverUrl: string | null }[]): string | null {
  return [...spots].filter((s) => s.coverUrl).sort((a, b) => a.order - b.order)[0]?.coverUrl ?? null;
}
```

**메타 추론 신설** (`lib/plan/infer-plan-meta.ts`, 35줄):
```ts
// 0495: region/movie 미입력 시 플랜이 담은 Spot에서 커버 후보용 시·도·작품을 추론.
//   저장값을 덮지 않는다 — pickPlanCover 입력 보강 전용.

// 최빈값 — 동률은 tieBreak로 결정적 선택(입력 순서 비의존).
export function inferRegionKey(addresses)  // 동률은 PROVINCES 표준 순서
export function inferMovieTitle(titles)    // 동률은 가나다순
```

**선택 순서 재배치** (`actions.ts`): `pickPlanCover` 단독 호출 → `resolveReuse` 먼저,
그 결과로 `resolveCover`:
```ts
// 재사용 판정을 먼저(주소·재사용 spotId 확보 → 커버 선택에 사용). tx 밖 read.
const resolvedItems = await resolveReuse(payload.items);
// 커버는 생성 시 1회만 부여. 우선순위: 담은 Spot 커버 → 작품 → 지역 → null (resolveCover).
const coverUrl = await resolveCover(payload, resolvedItems);
```

```ts
// 폴백: region/movie 비었거나 해석 실패면 담은 Spot에서 추론(사용자 입력 우선, 저장 필드는 미변경).
const region = normalizeRegionKey(payload.region) ? payload.region : inferRegionKey(items.map((i) => i.address));
const movie  = payload.movie?.trim() ? payload.movie : inferMovieTitle(reusedSpots.flatMap(...));
```

**백필 스크립트 동조** (`prisma/backfill-plan-covers.ts`): 같은 우선순위·추론을 반영하고,
로그에 `region=입력→추론` 형태와 `[자기커버]` kind를 추가.

### 0497 — 작성자 선택 UI

**후보 조회 서버 액션** (`actions.ts`):
```ts
// 0497: 대표 이미지 후보 조회 — 담은 좌표 항목을 200m+이름으로 Spot 해소, 커버 있는 재사용 Spot의
//   {coverUrl,name}을 order순 수집(coverUrl 중복 제거).
//   findNearbySpots가 자체 인증 가드 → 미인증이면 throw. 클라는 실패를 빈 후보로 흡수.
export async function getPlanCoverCandidates(items: { name; lat; lng }[]): Promise<{ coverUrl; name }[]>
```

**`CoverPicker.tsx` (신규 81줄)**:
```tsx
const DEBOUNCE_MS = 400;
// 좌표 항목 키 — 이 값이 바뀔 때만 재조회(제목·금액 타이핑 등과 무관).
const key = JSON.stringify(items.map((i) => [i.name, i.lat, i.lng]));
...
// 목표: 후보 2개 이상일 때만 블록 표시.
if (candidates.length < 2) return null;
```

선택 표시: 3px `border-primary` + 미선택 `opacity-50`, 다시 누르면 해제(null = 자동).
104×72 썸네일 가로 스크롤, `aria-pressed`.

**저장 경로**:
```ts
// 0497: 작성자가 고른 값 우선, 없으면 자동(담은 Spot 커버 → 작품 → 지역 → null).
const coverUrl = payload.coverUrl ?? (await resolveCover(payload, resolvedItems));
```
```ts
// 0497: 기본은 coverUrl 미접촉(생성 1회 원칙). 단 작성자가 고른 값이 오면 그때만 갱신(예외).
...(payload.coverUrl ? { coverUrl: payload.coverUrl } : {}),
```

수정 화면은 `coverUrl: plan.coverUrl`로 기존 커버를 선택 상태로 복원.

보안 메모(코드에 명시): `payload.coverUrl`은 "후보에서만 오지만 클라값이라 MVP는 신뢰" — 서버 검증 없음.

---

## 5. 결과 / 실측

- 커버 우선순위 최종형: **담은 Spot 커버(order 최소) → 작품 → 지역 → null**.
- 후보 조회 디바운스 400ms, 재조회 트리거는 좌표 항목 키 변경 시에만.
- 검증 수치 기록 없음(백필 스크립트 실행 결과 수치가 커밋 본문·주석에 남아 있지 않음).

### 이후 사이클 (사실 기록)

- `CoverPicker.tsx`는 **0562(`ef3accb`)에서 폐기** — "일정 행 재조판·썸네일 대표 지정"으로 대체.
- `lib/plan/infer-plan-meta.ts`·`pick-cover.ts`·`backfill-plan-covers.ts`는 현재도 존재.

---

## 6. 배운점

