# 0027 회고: 대시보드 UI 정리

**작성일**: 2026-05-16
**소요 시간**: 약 2시간
**관련 커밋**: feat - 0027 대시보드 UI 정리 (위젯 + 본문 + 헤더 사용자 정보 + 로그아웃 버튼 + 호버효과)

---

## 1. 한 줄 요약

대시보드 본문/위젯 통합 + (protected) layout에 공용 헤더 적용 + 카드 유형 배지(TIL/DeepDive/TechStudy) 적용 + 클릭 가능 요소에 호버 효과 적용.

---

## 2. 왜 / 목적 / 이유

### 헤더/네비게이션 = (protected) layout에 적용
- **왜**: 대시보드 외 페이지에서 헤더가 없거나 임시로 작성되어 있었음.
- **목적**: 로그인 사용자가 어느 페이지에서도 자기 정보 + 로그아웃이 있는 헤더가 보이게 만듦.
- **이유**: (protected) layout에 적용 = 7개 페이지에 1번만 작성. 페이지별 작성 = 7번 중복 + 수정 비용 큼. "수정 비용이 비싸지는 시점"이 공용 컴포넌트 적용 시점.

### 카드 유형 배지 적용
- **왜**: 모든 기록 카드가 같은 모양이라 TIL/DeepDive/TechStudy 구분 어려움.
- **목적**: 카드를 클릭하지 않고도 유형을 한눈에 알 수 있는 시각 신호 적용.
- **이유**: 글자색만 적용 = 신호 약함 / 색약 사용자 구분 어려움. 배경색 전체 변경 = 시각 노이즈 큼 / 카드 본질이 색에 묻힘. 배지 = 적절한 신호 강도 + 본질 유지. 색 + 텍스트 = 색약 사용자도 구분 가능.

### 호버 효과 = 클릭 가능 요소만
- **왜**: 카드 / 버튼이 클릭 가능한지 사용자가 알기 어려움.
- **목적**: 클릭 가능 요소에 시각 신호 적용해서 사용자 학습 유도.
- **이유**: 모든 요소에 일괄 적용 = 신호 기능 상실 (혼란 유발). 클릭 가능 요소만 적용 = "호버 = 클릭 가능" 자연스러운 학습. 호버는 신호이지 장식이 아님.

---

## 3. 작성한 프롬프트

```
[배경]
이전 0026에서 7개 페이지의 기본 디자인을 통일했으나,
대시보드 본문/위젯이 따로 배치되어 있고, 헤더에 사용자 정보/로그아웃이 없음.
작성한 기록들이 모두 같은 모양이라 유형 구분 어려움.
또한 클릭 가능한 요소들이 시각적으로 구분되지 않음.

[목표]
대시보드 본문/위젯 통합 + (protected) layout에 공용 헤더 적용 + 
카드 유형 배지(TIL/DeepDive/TechStudy) 적용 + 클릭 가능 요소에 호버 효과 적용.

[하지 말 것]
- 헤더에 추가 메뉴(설정/프로필) ❌ (MVP 외)
- 다크모드 활성화 ❌ (스코프 외)
- 호버 효과 일괄 적용 ❌ (클릭 가능 요소만)
- 카드 배경색 자체 변경 ❌ (배지로만 유형 표시)

[참조 패턴]
- 라우트 그룹 layout: 0024에서 적용됨
- Server/Client Component 분리: 0024 StreakWidget 패턴
- 호버 효과 = 클릭 가능 요소만
- 유형 배지 = TIL=blue / DeepDive=emerald / TechStudy=rose

[검수 내용]
- (protected) layout에 헤더 적용
- Server Component(getUser) + Client Component(LogoutButton) 분리
- 배지 = badgeStyle 객체 매핑 + ?? fallback
- 호버 효과 = Link/Button만

plan 요청.
```

---

## 4. 코드 작성 & 수정

### 1. (protected) layout에 헤더 추가 (변경)

```typescript
// app/(protected)/layout.tsx
import Link from 'next/link';
import { createClient } from '@/lib/supabase/server';
import { LogoutButton } from '@/app/(protected)/dashboard/LogoutButton';
import { signOutAction } from '@/app/(protected)/dashboard/actions';

export default async function ProtectedLayout({ children }: { children: React.ReactNode }) {
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  const initial = user?.email?.charAt(0).toUpperCase() ?? '?';

  return (
    <div className="min-h-screen bg-slate-50">
      <header className="sticky top-0 z-10 bg-white border-b border-slate-200">
        <div className="max-w-5xl mx-auto px-6 h-14 flex items-center justify-between">
          <Link href="/dashboard" className="text-lg font-bold text-slate-800">DevLog</Link>
          <nav className="flex items-center gap-4">
            <Link href="/dashboard" className="text-sm text-slate-600 hover:text-slate-900 transition-colors">대시보드</Link>
            <Link href="/til" className="text-sm text-slate-600 hover:text-slate-900 transition-colors">TIL</Link>
            <div className="flex items-center gap-2">
              <div className="w-7 h-7 rounded-full bg-slate-200 text-slate-700 flex items-center justify-center text-xs font-medium">
                {initial}
              </div>
              <span className="text-sm text-slate-600">{user?.email}</span>
            </div>
            <LogoutButton action={signOutAction} />
          </nav>
        </div>
      </header>
      <main>{children}</main>
    </div>
  );
}
```

### 2. LogoutButton 호버 효과 적용 (변경)

```typescript
// app/(protected)/dashboard/LogoutButton.tsx
// 변경 전
<button type="submit">로그아웃</button>

// 변경 후
<button 
  type="submit" 
  className="text-sm text-slate-700 bg-slate-100 hover:bg-slate-200 hover:text-slate-900 rounded-md px-3 py-1 transition-colors"
>
  로그아웃
</button>
```

### 3. 대시보드 본문 통합 + 카드 유형 배지 (대규모 변경)

```typescript
// app/(protected)/dashboard/page.tsx
import { getTilEntries } from '@/lib/til/actions';
import StreakWidget from './StreakWidget';
import Link from 'next/link';

type TilEntry = {
  id: string;
  title: string;
  content: string;
  created_at: string;
  type: 'TIL' | 'DeepDive' | 'TechStudy';
};

// 유형별 배지 스타일 매핑
const badgeStyle: Record<string, string> = {
  TIL:       'bg-blue-100 text-blue-700',
  DeepDive:  'bg-emerald-100 text-emerald-700',
  TechStudy: 'bg-rose-100 text-rose-700',
};

export default async function DashboardPage() {
  const entriesResult = await getTilEntries();
  const entries: TilEntry[] = 'error' in entriesResult
    ? []
    : (entriesResult.data.entries as TilEntry[]).slice(0, 6);

  return (
    <div className="flex flex-col gap-8">
      <div className="max-w-xs">
        <StreakWidget />
      </div>

      <section>
        <div className="flex justify-between items-center mb-4">
          <h2 className="text-xl font-semibold text-slate-700">기록</h2>
          <Link
            href="/til/new"
            className="text-sm bg-slate-800 text-white rounded-lg px-4 py-2 hover:bg-slate-700 transition-colors"
          >
            TIL 작성
          </Link>
        </div>

        {entries.length === 0 ? (
          <p className="text-slate-500 text-sm py-8 text-center">아직 작성한 기록이 없습니다</p>
        ) : (
          <ul className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
            {entries.map((entry) => (
              <li key={entry.id}>
                <Link
                  href={`/til/${entry.id}`}
                  className="bg-white rounded-xl shadow-sm p-5 flex flex-col gap-2 hover:shadow-md hover:-translate-y-1 transition-all duration-500 ease-out block h-full"
                >
                  {/* 유형 배지 */}
                  <span className={`self-start text-xs font-medium rounded-md px-2 py-0.5 ${badgeStyle[entry.type] ?? badgeStyle['TIL']}`}>
                    {entry.type}
                  </span>
                  <h3 className="font-semibold text-slate-800 line-clamp-1">{entry.title}</h3>
                  <p className="text-sm text-slate-600 line-clamp-3 flex-1">{entry.content}</p>
                  <time className="text-xs text-slate-400">
                    {new Date(entry.created_at).toLocaleDateString('ko-KR')}
                  </time>
                </Link>
              </li>
            ))}
          </ul>
        )}
      </section>
    </div>
  );
}
```

### 4. TIL 카드 호버 효과 (변경)

```typescript
// app/(protected)/til/page.tsx
// 변경 전
<Link
  href={`/til/${entry.id}`}
  className="bg-white rounded-xl shadow-sm p-5 flex flex-col gap-2 hover:shadow-md transition-shadow block h-full"
>

// 변경 후
<Link
  href={`/til/${entry.id}`}
  className="bg-white rounded-xl shadow-sm p-5 flex flex-col gap-2 hover:shadow-md hover:-translate-y-1 transition-all duration-500 ease-out block h-full"
>
```

---

## 5. 결과 / 배운점

### 결과
- 7개 페이지 헤더 통일 완료. 로그인 사용자 = 어느 페이지에서도 자기 정보 + 로그아웃 노출.
- 기록 카드 = 한눈에 유형 구분 가능 (배지 + 색).
- 클릭 가능 요소 = 호버 효과로 신호 전달 명확.

### 배운점
- **공용 컴포넌트 적용 시점** = 수정 비용이 비싸지는 시점. 페이지 7개 = 7번 중복 작성 vs 1번. 늘어나기 전에 적용하는 게 핵심.
- **시각 신호의 균형** = 신호 강도 vs 본질 유지. 너무 강하면 본질이 묻히고, 너무 약하면 신호 기능 상실. 배지 = 적절한 강도.
- **호버 = 신호이지 장식이 아님** = 모든 곳에 적용하면 신호 기능 상실. 클릭 가능 요소에만 적용해야 사용자 학습 가능.

---

## 다음 작업
하네스 엔지니어링 관련 파일 수정 및 정리 (CLAUDE.md / DECISIONS.md / AGENTS.md)
