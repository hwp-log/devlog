# 0190 회고: 촬영지 시딩 — 스팟 45곳·작품 11편·장면 설명 54건

작성일: 2026-07-14 / 소요: 약 1.5시간 / 커밋: d0037db

## 1. 한 줄 요약
공공데이터 15,034행에서 45곳을 큐레이션해 프로덕션에 주입했다 — 다중 작품 스팟 7곳으로 어제 만든 다대다 구조가 처음 화면에 드러났다.

## 2. 왜 / 목적 / 이유
- **왜**: 스팟 6개로는 지도가 비어 보였다(10초 관문 실패). 클러스터링·칩·배지 +N이 전부 데이터 부재로 죽어 있었다.
- **목적**: 스토리 없이 Spot+SpotMovie만 순수 추가(더미 스토리 금지 — 가짜 콘텐츠를 넣지 않는다). 기존 데이터 무접촉.
- **이유**: 큐레이션 2중 화이트리스트(장소도, 작품도) — 장소만 거르면 덕수궁 돌담길에 27편이 붙어 배지가 "+26"이 된다. 공용 로케이션(다리·공항·자주 쓰는 식당)은 순례 가치가 없어 제외. "오징어게임"(붙임)은 기존 "오징어 게임"에 병합(시딩 시점 정규화가 가장 싸다).

## 3. 작성한 프롬프트
★★★★★ 프로덕션 주입 — 스크립트 전문·예상 건수 사전 보고, 실행 전 백업, 실행 후 대조(21→66, 6→60, description 54), 멱등 재실행 확인 요구.

## 4. 작성·수정한 코드
```
prisma/seed-spots.json  (큐레이션 45곳, 출처 명기)
prisma/seed-spots.ts    (Movie upsert → Spot 생성(storyId 생략) → SpotMovie+description, name+좌표4 멱등)
```

## 5. 결과 / 교훈
- 대조 전량 통과. 재실행 신규 0.
- **사고**: CC가 승인 응답 전에 1차 주입을 실행 — "오징어게임"이 잠깐 프로덕션에 들어갔다 정확히 되돌려짐(title 토큰만 치환, description 본문 보존). 투명 보고는 좋았으나 **"승인 전 쓰기 금지"를 프롬에 더 강하게 박아야 한다**는 교훈.
- 시딩은 콘텐츠이자 **로직 검증**이다 — 데이터가 늘자 숨어 있던 버그(교통 250분)가 드러났다(→0191).
