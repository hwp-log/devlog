@AGENTS.md

# Claude Code 특화 규칙

공통 규칙은 `AGENTS.md` 참조. 이 파일은 Claude Code 전용 본질만 작성.

---

## 1. Plan Mode 적용 시점

4단계 사이클 중 Plan Mode 적용 규칙:

- **1차 바이브**: Plan Mode 사용 안 함 (기능 단위로 빠르게 진행)
- **2차 TDD-Green 작동 확인**: 테스트 영역만 Plan Mode 적용
- **3차 TDD 리팩토링**: Plan Mode 필수 (영역 → 플랜 → 컨펌 → 실행 → 검증)
- **4차 PR + 리뷰**: 변경 사항 요약 작성

---

## 2. "go" 명령어 흐름 (3차 TDD 리팩토링)

3차 리팩토링 시점에 사용자가 "go"라고 하면 다음 순서로 진행:

1. 다음 테스트 케이스 식별
2. 실패하는 테스트 작성 (Red)
3. 테스트 실행하여 실패 확인
4. 최소 구현 (Green)
5. 테스트 통과 확인
6. 리팩터링 (Refactor, 필요시)
7. 전체 테스트 실행

---

## 3. Code Classification & Intent Report

AI 코드 생성 후 다음 형식으로 분류 + 의도 보고한다.

**형식**:
```
생성된 코드:
- /path/to/file.ts (★★★★★ 등급명)
  → 검토 필수. 의도: [한 줄 설명]
- /path/to/another.tsx (★ 보일러플레이트)
  → 스킵 가능. 의도: [한 줄 설명]
```

**근거**:
- 사용자 시간 = 비즈니스 로직 검토에 집중
- 보일러플레이트 = AI에게 위임 가능
- AI 시대 = 외우는 능력 X / 판단 능력 O

> 검토 우선순위 등급 (★★★★★ ~ ★)은 `AGENTS.md` > `Review Priority` 참조.

---

## 4. @import 참조 규칙

이 파일 상단의 `@AGENTS.md`는 Claude Code의 `@import` 문법.

- 세션 시작 시 자동 로드됨
- 이후 이 파일 내용 추가됨
- 다른 AI 도구는 일반 텍스트로 처리

---

## 5. 모바일 기준선

모바일 대응 시 준수할 최소 기준. 신규 컴포넌트/페이지 작성 시 적용.
기존 위반 지점의 일괄 수정은 별도 사이클에서 진행하고, 이 문서는 기준만 정의.

### 터치 타겟
- 최소 크기 44×44px 이상 (Apple HIG 기준)
- 하단 고정 영역(탭바 등) 항목은 46px 이상 권장 (엄지 정확도 여유)
- 인접 타겟 간 간격 8px 이상 (오터치 방지)
- 주요 터치 요소는 화면 좌우 가장자리에서 16px 이상 이격

### 폰트 크기
- 입력 필드(input/textarea)는 모바일에서 16px 이상 (iOS Safari 자동 확대 방지)
- 본문 기본 16px, 보조 14px, **12px 미만 금지** (가독성 하한)
- 한국어 제목/헤딩 텍스트는 `break-keep` (word-break: keep-all) 적용 — 어절 단위 줄바꿈으로 한 글자 고아 방지

### 레이아웃
- 320px 폭에서 가로 스크롤 없이 reflow 가능 (WCAG 1.4.10)
- 하단 고정 요소는 `env(safe-area-inset-bottom)` 합산 (iOS 홈바 대응)

### 뷰포트 높이 (0253~0256 실측 교훈 반영)
- 고정 레이아웃 기본은 `svh`, `vh` 폴백 유지. `dvh`는 명시적 이유 있을 때만.
- **실기기 iOS Safari의 svh는 헤드리스·시뮬보다 작다** (주소창·툴바 몫).
  svh 기반 높이엔 `max()` 하한을 짝으로 고려한다.
- **중첩 flex 스크롤 컨테이너에 flex-grow 사이징 금지** — iOS Safari가
  grow를 계산하지 않아 한 줄 높이로 붕괴한 실측 이력(0253).
  스크롤 영역 높이는 명시 calc로 결정한다.
- 서로 파생 관계인 높이 상수(시트↔목록 등)는 콜로케이션 + 상호 참조
  주석으로 짝을 명시한다 ("한쪽만 바꾸면 클립 잘림").
- Tailwind arbitrary 값은 완전 리터럴만 JIT 스캔됨 — 상수 추출 불가 시
  주석으로 동기 관계를 남긴다.

### 검증 폭
- 360px / 390px / 768px 세 지점에서 확인
- 최종 실기기(iOS Safari + Android Chrome)에서 주소창 노출/숨김 상태 교차 확인

---

## 6. 문서 번호 체계

**원칙: 한 작업 = 한 NNNN = 한 회고.** 작업의 커밋(코드·docs)에는
해당 번호를 부여한다 (예: `feat: 0163 ...` / `docs: 0163 회고 - ...`).

예외:
- 여러 소작업을 하나의 회고로 묶을 때: 회고가 범위를 명시 (예: 0153-0156)
- 준비성 커밋(chore 의존성 추가 등)이 번호 없이 선행될 수 있음 —
  회고의 "관련 커밋"에 해시로 편입
- 분석·기획은 작업 매핑이 없으므로 NNNN이 아닌 `ANNN`
  (`docs/analysis/`, 독립 순번)
- 결번(0158·0159)은 재사용하지 않고 이관 표지 파일을 둔다

---

## 7. 회고 작성 정책

### 작성 시점 (트리거)
- **작업 단위 완료 보고에 회고 초안 작성 여부를 반드시 포함한다** —
  "회고 초안 작성했습니다" 또는 "회고 대상 아님(사유)" 중 하나.
  누락은 보고 미완으로 간주.
- 작업 단위 = plan 1개 승인~검증 완료의 묶음. 소작업 여러 개가 한
  기능이면 마지막 완료 시점에 범위를 묶어 1편.

### 양식·번호
- `docs/prompts/0999-회고-작성-예시.md`를 먼저 읽고 그 구조·톤을 따른다
  (5섹션 경량 양식).
- 파일명: `docs/prompts/NNNN-한국어-제목.md`.
  번호는 추측 금지 — ① `ls docs/prompts/`로 기존 회고 최신 번호,
  ② `git log --oneline`로 대상 커밋 범위를 둘 다 대조해 확정한다.
  결번·자리표시자 이력("025X"=0252, 0268 결번 등)은 그대로 명기.

### 역할 분담
- 사실 섹션(한 줄 요약·프롬프트·코드·커밋 목록)은 CC가 채운다.
- **"왜/목적/이유"·"배운점"은 `[사용자 확인 필요]` 표시 + 초안 수준까지만.**
  git log에 "왜"는 없다 — 지어내지 말고 비워두는 것이 정답이다.
- 초안은 커밋하지 않고 보고 — 사용자 검토·수정 후 사용자가 커밋.

### 회고-코드 동기 (부패 방지)
- **기존 커밋 범위를 수정하는 작업**(버그 수정·revert·값 변경) 시:
  `docs/prompts/`에서 해당 번호 범위를 다루는 회고 존재를 확인하고,
  있으면 회고의 사실 서술(코드 블록·결과)도 함께 정정한다.
  정정 불가·불명확 시 그 사실을 보고에 명시한다 (조용한 어긋남 금지).

---

## 8. 완료 보고 전 원칙 정합 자가점검

새로 만들거나 수정한 코드가 레포의 기존 원칙과 정합하는지 확인하고
보고에 명시한다.

최소 점검 항목:
- ① 저장·삭제·반영의 시점이 2단 저장 구조(팝업/로컬 = 임시, 상단 제출 =
  영속)와 일치하는가
- ② 같은 사실을 두 곳에 기록하는 상태·필드를 새로 만들지 않았는가
  (단일 소스 + 파생)
- ③ 이번 작업에서 세운 원칙이 있다면 그 원칙이 적용돼야 할 다른 층이
  없는가

위반 발견 시 임의 수정하지 말고 보고만.

---

## 9. 포트폴리오의 UX 표준

Dotrip은 토이가 아니라 취업용 제품이다. 화면·기능을 막론하고 큰 틀은 업계 표준을 따른다.

- 레이아웃·내비게이션·핵심 인터랙션 패턴을 자의적으로 설계하지 않는다. plan 전에 표준 패턴을 web_search로 확인하고, 시안·구현이 표준과 맞는지 대조한다. 벗어나면 근거를 명시한다.
- 근거: (1) 사용자가 이미 학습한 패턴이라 학습 비용이 없다 (2) "왜 이 구조인가"를 표준과 검증 근거로 방어할 수 있다.
- 안티패턴도 표준이다. 업계가 지양하는 패턴은 채택하지 않는다.
- 세부 결정(어떤 데이터를 어떻게 보여줄지)은 표준 강제 대상이 아니다. 서비스 성격에 맞게 정한다.
- 데이터 없는 필드는 넣지 않고, 있는 필드로 대체한다.
- 상태 표시의 위계: 복수 상태(선택·호버 등)가 공존할 땐 둘 다 표시하되
  우선순위로 강조를 차등한다 (Material 상태 가이드). 일시 상태(호버)는
  무채·명도 반응으로 낮추고, 지속 상태(선택)에만 색을 배정한다 —
  색이 늘수록 상태 언어가 경쟁한다 (0267~0272 실측 확정).

---

## 10. 화면 폭 규칙 (0534~0536 확립)

판단 기준은 하나다: **그 화면에서 사용자가 뭘 하는가.**
고르는 화면은 넓어야 후보를 많이 보고, 입출력 화면은 좁아야 집중이 유지된다.

- ① **고르는 화면(목록·지도) = 풀블리드** (max-w 없음, 패딩만)
- ② **그 외 전부(읽기·작성·설정·관리) = 860** (`--reading-w`)

### 정본 위치 (이 문서에 값을 복제하지 않는다 — 포인터만)
- 라우트 갈래 목록: `app/(protected)/_components/ProtectedMain.tsx`
  (`FULL_BLEED_ROUTES` / `WIDE_ROUTES`, wide는 정확 매칭 — 상세·작성은 자동으로 제외)
- 폭 값: `app/globals.css`의 `--reading-w`
  (860 = 0322 실화면 판정. 검증 문헌 대역 680~780보다 실판정 우선.
  `--story-content-w`는 파생 별칭)

### 헤더·푸터
**항상 고정 형태 — 라우트별 가변 금지.** 본문 분기에 맞추는 안은 0534~0536에서
설계까지 갔다가 취소됐다: 정렬선 불일치는 사실이나 사용자가 겪는 문제가 아니었고,
고치면 라우트 이동마다 헤더 폭이 변하는 없던 불편이 생긴다. 재제안하지 않는다.

### 새 화면 추가 시 확인 순서
1. 어느 갈래인지 판정 (사용자가 고르는가, 입출력하는가)
2. 해당 폭 적용 (풀블리드면 WIDE_ROUTES 편입 / 그 외 `max-w-[var(--reading-w)] mx-auto`)
3. 그리드가 있으면 **그 컨테이너 기준으로 bp 재계산** — 숫자 복사 금지
   (0530에서 복사했다가 320 하한을 깬 전례. 산출식은 각 목록 그리드 주석의 0425 형식)
4. 이미지 `sizes` 정합 확인 — 폭이 바뀌면 sizes가 화석이 된다
   (0534 히어로 640px·0535 카드 400px/50vw 세 번 다 나온 유형)

### 잔여 미편입 (규칙 확립 시점 기준)
- `/my-plan/[id]` — 읽기 화면인데 1232. 위계 정돈 사이클에 폭도 함께 편입
- `/til` — 목록인데 기본 분기 + 구식 그리드 (0532부터 미정합 보고 누적)
- auth(로그인·가입) — (protected) 밖이라 규칙 적용 여부 자체가 미판정

---

## 11. 진입 로딩 스켈레톤 규칙 (0490·0491·0538·0539 확립, 뿌리는 0413~0414)

판단 기준은 하나다: **화면의 틀이 데이터에 의존하는가.**
(0413 "스켈레톤은 '아직 모르는 것'에만 씌운다 — 정적 텍스트를 셔머로 덮으면
같은 글자 재출현 = 깜빡임"의 일반화)

- ① **의존함** (남의 글·남의 코스 — 제목·본문·사진이 전부 데이터)
  → **골격 전체 shimmer** (스토리 상세 0490 / 플랜파인더 상세 0538)
- ② **의존 안 함** (틀이 고정이고 값만 들어옴 — 설정·작성 폼·목록 헤더)
  → **틀은 실물, 데이터 자리만 shimmer** (스토리 작성·수정 0491 / MyPage 0539 /
  플랜파인더 목록 헤더 0413~0414). 전체 회색 덩어리 금지 — 뭐가 뜰지 아는
  화면은 틀을 미리 그린다.
  - ②에서 지킬 것: **데이터가 0인 블록은 통째 실물** (0539 비밀번호 변경·
    계정 삭제 카드 — 초기값이 빈 칸/접힘이라 기다릴 게 없음)

### 공통 원칙 (0491 주석 정본의 명문화)
- 같은 폼·같은 치수면 스켈레톤 1벌 공유 (StoryWriteSkeleton을 new·edit 공유)
- 조건부 블록: **지배적 대표형(대부분의 실데이터에서 뜨는 블록)은 포함**
  (0538 비용·항공 — "대표형 = 전부 있음"), **드묾·뷰포트 밖·소유자 조건부는
  생략** (0490 수정·삭제 아이콘, 0491 PLAN 블록)
- 실화면 리터럴 준용 + 상호 참조 주석 — "한쪽만 바꾸면 스켈레톤이 어긋남"을
  양쪽에 명시. 대표 개수는 쿼리 상한·대표형 근거를 주석으로 남긴다
- 셔머는 기존 `skeleton-shimmer` 유틸만 재사용
- 줄높이 산식 주의: Tailwind 폰트 유틸(text-lg 등)은 lh 내장, arbitrary
  (text-[20px])는 lh 상속(1.5) — 바는 실물 줄높이 박스 안에 넣어야 시프트가
  없다 (0539 실측)

### 금지
- 콘텐츠 인위 지연 (0489에서 안티패턴으로 기각 — 재제안하지 않는다)
- 새 애니메이션 방식

### 정본 위치 (이 문서에 값을 복제하지 않는다 — 포인터만)
- 셔머 유틸: `app/globals.css`의 `@utility skeleton-shimmer`
- 각 방식의 상세 근거: 해당 `loading.tsx` 상단 주석
  (plan-finder/(list) 0413~0414 · story/[id] 0490 · story/new 0491 ·
  plan-finder/[id] 0538 · mypage 0539)

### 신규 화면에 로딩 붙일 때 확인 순서
1. 갈래 판정 — 틀이 데이터에 의존하는가 (①인가 ②인가)
2. 폭은 §10 갈래·토큰 그대로 (스켈레톤도 같은 래퍼·같은 `--reading-w`)
3. 실화면 클래스 준용 — 대표 개수를 정하고 근거(쿼리 상한 등)를 주석에
4. 전환 시 레이아웃 시프트 확인 (줄높이 산식 주의)
5. 상호 참조 주석 — 실화면 짝 블록 명시

### 정합 확인 이력
- plan-finder/(list)는 0535 풀폭 편입 정합 확인 (0540 검증 — 그리드 클래스가
  실목록과 동일, 카드 수 `PLAN_PAGE_SIZE` 공유)