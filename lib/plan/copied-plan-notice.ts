// 0596: 담은 플랜에 보여주는 안내 문구의 정본.
//
// **판정과 층이 다르다.** 표시 조건은 lib/plan/cost-snapshot.ts(costEdited 대조, 0595)가
// 갖고, 이 파일은 그 결과를 사람에게 보여주는 말만 갖는다. 한 파일에 두면 문구가 늘거나
// 조건과 무관한 안내가 생길 때 그 파일이 두 가지 일을 하게 된다(사용자 판단).
//
// 두 화면(읽기 상세 PlanFinderDetail · 편집 폼 MyPlanNewForm)이 이 상수를 **참조**한다.
// 0579~0590 동안은 같은 문자열이 두 곳에 각각 박혀 있었고 "리터럴 준용" 주석으로 사람이
// 동기화를 지켰다 — 0596에서 그 부담을 없앴다.

/**
 * 담은 플랜의 비용 배너 문구.
 *
 * 구 문구("원본 작성 시점의 금액입니다. 현시점에서는 다를 수 있으니, 확인 바랍니다.")는
 * **우리가 모르는 것을 단정했다.** 원본 작성자가 그 금액을 언제 적었는지 알 방법이 없어서,
 * 어제 올라온 플랜을 오늘 담아도 "현시점에서는 다를 수 있다"가 그대로 나갔다.
 * 매번 같은 말이 뜨면 사용자는 "담으면 원래 뜨는 것"으로 학습하고 문구를 안 읽게 된다.
 *
 * 지금 문장이 지키는 두 가지:
 *   ① **시간을 언급하지 않는다** — 언제 담았든 항상 참이다.
 *   ② **할 일을 명시한다** — "확인 바랍니다"는 무엇을 하라는지 불분명했다.
 *
 * 참고: 0593은 "공개 시점에는 '원본 작성 시점의 금액입니다'가 틀린 말이 된다"를 근거로
 * 표시 조건을 비공개 한정으로 좁혔는데, 새 문구엔 그 논리가 성립하지 않는다. 다만 그 조건은
 * 0595에서 이미 costEdited 기준으로 대체돼 실질 영향은 없다(회고는 시점 기록이라 미수정).
 */
export const COPIED_PLAN_COST_NOTICE =
  '원본 작성자가 입력한 금액입니다. 내 여행에 맞게 수정해보세요.';
