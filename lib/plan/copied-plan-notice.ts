// 0596: 담은 플랜에 보여주는 안내 문구의 정본.
//
// **판정과 층이 다르다.** 표시 조건은 lib/plan/cost-snapshot.ts(costEdited 대조, 0595)가
// 갖고, 이 파일은 그 결과를 사람에게 보여주는 말만 갖는다. 한 파일에 두면 문구가 늘거나
// 조건과 무관한 안내가 생길 때 그 파일이 두 가지 일을 하게 된다(사용자 판단).
//
// 두 화면(읽기 상세 PlanFinderDetail · 편집 폼 MyPlanNewForm)이 이 상수를 **참조**한다.
// 0579~0590 동안은 같은 문자열이 두 곳에 각각 박혀 있었고 "리터럴 준용" 주석으로 사람이
// 동기화를 지켰다 — 0596에서 그 부담을 없앴다.
//
// 0597: 한 줄 → **제목·설명 2단**으로 분리(배너가 경고 면에서 안내 블록으로 바뀌며).
//   상수도 둘로 나눈다 — 한 문자열에 개행을 넣어 화면에서 자르는 방식은 두 화면이 자르는
//   규칙을 각자 갖게 되므로(그게 0596에서 없앤 부담이다) 처음부터 두 값으로 둔다.
//
// 0598: 두 줄의 **내용과 순서를 재배정**했다(실화면 검수). 상수 이름 TITLE/BODY는
//   위/아래 줄을 가리키는 **구조적 이름**이라 그대로 두고, 담기는 내용을 바꿨다 —
//   값만 맞바꾸면 아래 JSDoc이 실제 역할과 어긋난다.

/**
 * 1행(위) — **사실**. 13px / 500 / fg.
 *
 * 0598: 구 1행은 할 일("내 여행에 맞게 금액을 수정해보세요")이었는데 순서를 뒤집었다.
 * **사실이 먼저, 할 일이 뒤**다 — 왜 이 안내가 떴는지를 먼저 말해야 아래 줄의 지시가
 * 근거를 갖는다. 굵은 위 줄이 "이 금액은 내가 정한 게 아니다"를 먼저 알린다.
 */
export const COPIED_PLAN_COST_NOTICE_TITLE = '원본 작성자가 입력한 금액입니다.';

/**
 * 2행(아래) — **할 일**. 12.5px / fg2 / line-height 1.5.
 *
 * 0596 원칙 유지: **시간을 언급하지 않는다.** 원본 작성자가 그 금액을 언제 적었는지 알 방법이
 * 없으므로 "낡았다"고 단정할 수 없다(구 문구 "현시점에서는 다를 수 있으니"의 기각 사유).
 *
 * 0598 변경 둘:
 *   ① 구 2행("인원과 일정에 따라 실제 비용은 달라집니다")은 **삭제**했다 — 누구나 아는
 *      내용이라 자리를 차지할 이유가 없다.
 *   ② **"수정해 저장해보세요"** — 저장해야 반영된다는 것을 한 단어로 알린다.
 *      판정이 저장 기준이라(0595) 편집 폼에서 금액을 고치는 중에도 배너가 남는데,
 *      그게 "저장 전이라 그렇다"는 것을 사용자가 추론해야 했다. 문구가 그 간극을 메운다 —
 *      **문구가 시키는 일과 시스템이 감지하는 일을 일치시킨다**(0596 배운점의 연장).
 */
export const COPIED_PLAN_COST_NOTICE_BODY = '내 여행에 맞게 금액을 수정해 저장해보세요.';
