import 'server-only';

// 0601: 담기 가격. **주문 금액을 정하는 산출은 이 파일 하나뿐이다** —
//   다른 곳에서 숫자를 다시 쓰지 않는다. 결제 검증(승인 단계)이
//   "서버가 정한 금액 == successUrl로 돌아온 금액"으로 성립하려면 산출점이 하나여야 한다.
//   두 곳에서 계산하면 둘이 어긋나는 순간 검증이 통과하는 조작 경로가 생긴다.
//
//   MyPlan.sourceCostTotal(담은 시점 비용 합, 0594)과는 **뜻이 다른 값**이다.
//   저건 "이 여행에 얼마가 들었나"(원본 작성자가 적은 여행 경비)이고,
//   이건 "이 플랜을 담는 데 우리에게 얼마를 내는가"(서비스 이용료)다.
//   금액이 우연히 비슷해져도 서로 참조하지 말 것.
//
//   'server-only': 이 상수가 클라이언트 번들로 새는 경로를 컴파일 단계에서 막는다
//   (lib/flights/client.ts와 같은 장치). 금액은 서버가 정해 내려보내는 값이지
//   클라이언트가 읽어 쓰는 값이 아니다.
//   0603: 화면에 보이는 금액 문구는 CopyPlanFinderButton의 PRICE_LABEL('1,500원')에 따로 있다
//   (이 파일이 server-only라 클라이언트에서 import할 수 없다). **값을 바꾸면 그쪽도 함께 고칠 것** —
//   한쪽만 바꾸면 화면 금액과 실제 청구 금액이 갈린다.
export const PLAN_COPY_PRICE = 1500;
export const PLAN_COPY_CURRENCY = 'KRW' as const;
